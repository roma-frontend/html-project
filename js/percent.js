const lineImage = document.getElementById("line-image"), circle = document.getElementById("circle"), percentText = document.querySelector(".line-title__percent"); let isDragging = !1; circle.addEventListener("mousedown", e => { isDragging = !0, e.preventDefault() }), document.addEventListener("mousemove", e => { if (isDragging) { let t = lineImage.getBoundingClientRect(), n = (e.clientX - t.left) / t.width * 100, i = Math.min(100, Math.max(0, n)); circle.style.right = `${97 - i}%`, percentText.textContent = `${i.toFixed(2)}%` } }), document.addEventListener("mouseup", () => { isDragging = !1 });